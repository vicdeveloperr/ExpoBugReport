import { whiteColor } from "./colors";
import type { StyleProp, ViewStyle } from "react-native";

export const centerViewContentStyle: StyleProp<ViewStyle> = {
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
};

export const paragraph = {
    color: whiteColor,
    fontSize: 20
}